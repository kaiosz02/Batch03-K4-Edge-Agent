# Reflection cá nhân — Tạ Hồng Quí

**MSSV:** 2A202601538
**Vai trò:** Backend Developer

## Phần công việc tôi đã thực hiện

Tôi phụ trách xây dựng toàn bộ backend FastAPI của V-Pet Tutor, bao gồm API tải
slide, sinh và nộp Adaptive Quiz, quản lý trạng thái Pet/EXP, ghi telemetry và
tổng hợp Knowledge Heatmap. Tôi tích hợp DeepSeek để sinh câu hỏi bám theo đoạn
văn bản học viên bôi đen, đồng thời giữ đáp án và xử lý chấm điểm hoàn toàn ở
backend để frontend không thể tự gửi kết quả hay tự cộng EXP. Tôi cũng bổ sung
validation cho request, lưu lịch sử phiên học để điều chỉnh độ khó, chống nộp
một quiz nhiều lần và viết smoke test cho luồng highlight → quiz → EXP →
heatmap.

## AI đã hỗ trợ tôi như thế nào?

Tôi sử dụng AI coding assistant để gợi ý cấu trúc API, rà soát các trường hợp
biên và hỗ trợ debug nhanh. Tôi trực tiếp kiểm tra lại luồng dữ liệu, chỉnh sửa
code, chạy test và đối chiếu response giữa frontend với backend trước khi tích
hợp.

## Bài học từ case thất bại

Ở phiên bản đầu, nếu người dùng gửi lại cùng một `quiz_id`, hệ thống có nguy cơ
cộng EXP nhiều lần. Tôi đã chuyển sang lấy và xóa quiz theo thao tác atomic có
lock, đồng thời không tin các giá trị `is_correct` hoặc `exp` từ client. Qua đó
tôi rút ra rằng với gamification, backend phải là nguồn sự thật duy nhất và các
luồng có phần thưởng luôn cần được kiểm thử cả trường hợp gian lận lẫn request
đồng thời.






